---
description: 牛马公司 v4 — 公司式多Agent部门协作执行系统
argument-hint: <任务描述>
---

# 🐂🐎 牛马公司 v4

**核心原则：项目经理（主窗口）只负责两件事——接收任务和交付结果。所有执行工作在 Agent 层完成，通过文件中介传递信息，避免上下文堆积。**

**v4 新增：上下文预算事前列控 + 主管助理传声筒 + 动态保护接替机制 + 状态仪表盘**

## 引擎配置

牛马公司不绑定特定模型。所有角色分为两层，每层由你配置的 API Key 决定用什么模型。

### 干活引擎

项目经理、主管、主管助理、组长、操作员全部跑在 **Claude Code 底座**上，模型取决于你在 `~/.claude/settings.json` 里配的 API 提供商。

| 角色 | 方式 | 说明 |
|------|------|------|
| 项目经理（主窗口） | 主对话 | 只收任务、转发、展示结果，不参与执行 |
| 主管 | Agent 工具 | HR、组建团队、上下文预算分析、协调 REVISE |
| 主管助理 | Agent 工具 | 传声筒：只汇总各组报告，不做决策不改代码不参与 QA |
| 各部门组长 | Agent 工具 | 拆分任务、派出操作员、验收本部门产出 |
| 各部门操作员 | Agent 工具 | 实际写代码/文件，每人 ≤ 3 个产出文件 |

> 💡 干活引擎不用最聪明的模型，选性价比高的：DeepSeek V4（推荐）、Claude Sonnet、GPT-4o 等。

### 审查引擎

审查引擎独立于干活引擎，**用你手里最聪明的模型**来挑毛病。支持任意终端可调用的 AI 工具：

| 接入方式 | 命令示例 | 适用模型 |
|----------|---------|----------|
| Codex CLI | `codex exec` | OpenAI Codex（推荐） |
| Claude Opus Agent | Agent 工具 `model:"opus"` | Claude Opus / Mythos |
| Open Claw CLI | 该工具的命令行 | 任意 OpenAI 兼容模型 |
| 任意终端 AI | `某命令 < prompt.txt` | 任意模型 |

> 💡 审查引擎用你最聪明的模型：Codex（推荐）、Claude Opus、Claude Mythos、GPT-5 等。没有额外 CLI 工具时自动降级为 Claude Opus Agent。

### 审查引擎选择（可配置）

牛马公司不绑定特定审查工具。启动时按以下优先级选择：

```
1. 用户显式指定 → 用指定引擎
2. Codex CLI 已装且已登录 → 用 codex exec（推荐）
3. 其他 CLI（Open Claw 等）已装 → 用对应命令
4. 全都没有 → 降级为 Agent 工具 model:"opus"（内置，零配置）
```

**在每个 QA 审计阶段，先检测可用引擎，再用选中的引擎执行审查。Codex 不可用时自动降级，不影响流程。**

## 关键机制：文件中介

**核心思想：Agent 之间传文件路径，不传内容。**

```
没有文件中介（上下文堆积）：
  Agent A → 把3000字代码塞进 prompt → Agent B → 塞进 prompt → Agent C → 💥

有文件中介：
  Agent A → 写到 /tmp/se-task/{阶段名}/ → 只传路径 → Agent B 按需 Read → 不会爆
```

所有中间产出统一放到 `/tmp/se-task/` 目录树：

```
/tmp/se-task/
├── 00-context-budget.md           # 阶段一产出：上下文预算表
├── 01-department-plan.md          # 阶段一：主管部门方案
├── 02-team-splits.md              # 阶段二：各组拆分方案
├── .status/                       # Agent 状态文件（每Agent一行）
│   ├── 主管.status
│   ├── 主管助理.status
│   ├── 前端组组长.status
│   └── ...
├── .checkpoint/                   # 断点存档（动态保护触发时写入）
│   └── {角色名}.md
├── qa-reports/                    # QA 各阶段审计结果
├── departments/{部门名}/          # 各组产出目录
│   ├── SUMMARY.md                 # 组长汇总
│   └── ...                        # 操作员产出文件
├── 04-final-report.md             # 主管助理汇总的最终报告
├── 04-code-package.md             # 代码审查包
└── 05-delivery.md                 # 最终交付报告
```

---

## 组织架构

```
用户
  ↕ 只和项目经理对话
项目经理（主窗口 Claude）──── 只收任务、转发、展示
  │
  ├── 主管 Agent ──── HR、组建团队、预算分析、协调 REVISE
  │     ├── 前端组组长
  │     │     ├── UI 工程师
  │     │     └── 交互工程师
  │     ├── 后端组组长
  │     │     ├── API 工程师
  │     │     └── 数据库工程师
  │     ├── 测试组组长
  │     │     └── 测试工程师
  │     └── ...按需组建
  │
  ├── 主管助理 Agent ──── 传声筒：只汇总各组报告，不做决策不改代码
  │
  └── QA 审查专员 ──── 每次全新上下文
```

---

## 五阶段工作流程

```
用户 → 项目经理 → 主管Agent → QA-1 → 组长Agent → QA-2 → 操作员 → 主管助理汇总 → QA-3 → 交付
        (主窗口)    (Agent)   (Codex)  (Agent)   (Codex)  (Agent)    (Agent)     (Codex)  (主窗口)
```

**项目经理每阶段只做三件事：派 Agent → 收文件路径 → 转 Codex → 看结论。**

---

### 阶段零：项目经理接收任务

1. 项目经理（主 Claude）解析 `$ARGUMENTS`
2. 判断任务类型（Bug修复/新功能/重构/调研/其他）
3. 创建临时工作目录 `/tmp/se-task/` 及子目录 `.status/` `.checkpoint/`
4. 向用户确认："收到，目标：XXX，开始组建团队。"

---

### 阶段一：主管做 HR + 上下文预算（Agent）+ QA-1 审计（Codex）

#### 1.1 派出主管 Agent

```
你是项目主管。任务目标已由项目经理确认。现在你需要兼任 HR，
分析任务需求，组建项目团队。⚠️ 你的方案必须先通过 QA 审计。

任务目标：$ARGUMENTS

你需要完成：

## 一、部门矩阵
1. 分析任务需要哪些部门（前端组/后端组/测试组/文档组/数据组等）
2. 每个部门的职责范围
3. 每个部门指定一名组长
4. 预估每个部门需要的操作员人数（0-3人）
5. 部门间的协作依赖关系

## 二、上下文预算分析（🆕 v4 强制要求）

为每个角色预估上下文消耗，写入 /tmp/se-task/00-context-budget.md。

预算公式：预估用量 ≈ 任务描述字数 + 需读文件大小估算 + 产出文件大小估算 + 对话轮数预留

**文件大小估算基准：**
- 小型文件（配置/脚本）：~2K tokens
- 中型文件（单组件/单模块）：~5K tokens
- 大型文件（完整页面/核心逻辑）：~10K tokens
- 汇总报告（SUMMARY/报告类）：~8K tokens

风险分级（以 1M = 1,000,000 tokens 为基准，安全阈值用 800K）：
- 🟢 ≤60%（≤480K）：安全
- 🟡 61-80%（481K-640K）：临界，需说明风险缓解措施
- 🔴 >80%（>640K）：危险，必须拆人/加人/加部门，禁止直接分配

**主管自身也必须纳入预算分析。**

返回格式：
## 部门矩阵
| 部门 | 职责 | 组长 | 操作员数 | 依赖 |
|------|------|------|----------|------|

## 上下文预算表
| 角色 | 任务描述 | 需读文件 | 产出文件 | 预估对话轮数 | 预估总用量 | 风险 |
|------|----------|----------|----------|-------------|-----------|------|
| 主管 | ... | ... | ... | ... | ... | 🟢/🟡/🔴 |
| 主管助理 | ... | ... | ... | ... | ... | 🟢/🟡/🔴 |
| 前端组长 | ... | ... | ... | ... | ... | 🟢/🟡/🔴 |
| ... | ... | ... | ... | ... | ... | ... |

## 分配理由
（为什么这样组建，预算分析发现的特殊处置）

## 预计总人数
组长 N 人 + 操作员 M 人 + 主管助理 1 人 = 总计 K 人

## 预算文件
已写入 /tmp/se-task/00-context-budget.md
```

**项目经理收到后：**
- 写入 `/tmp/se-task/01-department-plan.md`
- 主管已写入 `/tmp/se-task/00-context-budget.md`（主管在 Agent 内 Write）

#### 1.2 QA-1 审计

**先检测可用审查引擎：**

```bash
# 检测 Codex CLI
which codex 2>/dev/null && echo "CODEX_OK" || echo "NO_CODEX"
```

**Codex 可用 → 用 Codex（推荐）：**

项目经理把方案文件路径传给 Codex（全新上下文）：

```bash
cat > /tmp/se-task/qa-reports/qa-1-result.txt << 'PROMPT_EOF'
你是极度严格的CTO。审核项目主管的部门组建方案，1-10打分。

任务目标：$ARGUMENTS

主管的部门方案见文件：/tmp/se-task/01-department-plan.md
上下文预算表见文件：/tmp/se-task/00-context-budget.md
请用 Read 工具打开这两个文件阅读完整内容后再给出评审。

审核要点：
- 部门设置是否合理？有没有遗漏或多余的部门？
- 部门职责边界是否清晰？有没有重叠或空白？
- 人员配置是否合适？多了还是少了？
- 🆕 上下文预算是否合理？预估是否准确？有没有 🔴 危险角色？风险缓解措施是否充分？

评分标准：
- 8-10分：方案合理 → APPROVE
- 5-7分：有明显问题 → REVISE
- 1-4分：方案有重大缺陷 → REVISE

返回格式：
## QA 审计 — 部门方案
**评分**：X/10
**问题**：...
**修改建议**：（每条具体可操作）
**预算审查**：预算是否合理，🔴角色如何处理
**决定**：✅ APPROVE / 🔄 REVISE
PROMPT_EOF
cat /tmp/se-task/qa-reports/qa-1-result.txt | codex exec --dangerously-bypass-approvals-and-sandbox
```

**Codex 不可用 → 降级为 Agent 工具 `model:"opus"`：**

```
你是极度严格的CTO。审核项目主管的部门组建方案，1-10打分。

任务目标：$ARGUMENTS
主管方案文件：/tmp/se-task/01-department-plan.md（请 Read 后评审）
上下文预算表：/tmp/se-task/00-context-budget.md（请 Read 后评审）

{其余 prompt 同上}
```

**处理：**
- ✅ APPROVE → 项目经理一句话通报用户（含 QA 评分 + 一句话预算概况），进入阶段二
- 🔄 REVISE → 项目经理把 QA 结果传给主管 Agent 修改，重新提交（最多3轮）

**项目经理向用户展示：** QA-1 评分 + 决定 + 预算风险概况（如"3🟢 1🟡，整体安全"）。

---

### 阶段二：组长拆分任务 + QA-2 审计（可选）

#### 2.1 并行派出组长 Agent

**所有组长并行派出，prompt：**

```
你是{部门名}的组长。主管已经分配了部门职责。你需要拆分任务并组建操作员团队。

项目目标：$ARGUMENTS
部门职责（来自主管方案）：{该部门的职责描述}
上下文预算表：/tmp/se-task/00-context-budget.md（参考你的预算额度）

⚠️ 约束：每个操作员的产出文件数 ≤ 3。超出必须拆人或加人。
⚠️ 预算意识：如果你预估自己的上下文接近临界，可以在任务拆分中标注。

你需要完成：
1. 把部门任务拆分成具体的子任务（2-5个）
2. 每个子任务分配操作员（可以是新建 Agent，也可以你自己做）
3. 根据任务量决定操作员人数（0-3人，0表示组长全包）
4. 制定交付标准

完成后，将自己的状态写入 /tmp/se-task/.status/{部门名}组长.status：
```
状态：工作中
部门：{部门名}
预估上下文用量：...K
当前阶段：阶段二（拆分任务）
```

返回格式：
## {部门名} 任务拆分
| # | 子任务 | 操作员 | 产出文件（≤3个） | 交付标准 |
|---|--------|--------|-----------------|----------|

## 团队配置
- 操作员总数：N（0 = 组长全包）
- 组长亲自负责：{列出子任务编号}
```

**项目经理收到后 → 合并写入 `/tmp/se-task/02-team-splits.md`**

#### 2.2 QA-2 审计（可选）

**跳过条件：总操作员 ≤ 3 人 且 部门 ≤ 2 个 → 跳过，直接进阶段三。**

**否则合并提交 Codex：**

```bash
cat > /tmp/se-task/qa-reports/qa-2-result.txt << 'PROMPT_EOF'
你是极度严格的CTO。审核各部门组长的任务拆分方案，1-10打分。

任务目标：$ARGUMENTS
主管方案：/tmp/se-task/01-department-plan.md
各组拆分方案：/tmp/se-task/02-team-splits.md
上下文预算表：/tmp/se-task/00-context-budget.md

审核要点：
- 拆分粒度是否合适？
- 操作员配置是否合理？每人产出文件是否 ≤ 3？
- 有没有遗漏的子任务？
- 交付标准是否清晰可验证？
- 🆕 拆分后的实际工作量是否与预算表一致？

返回格式：
## QA 审计 — 组长拆分方案
**评分**：X/10
**问题**：...
**修改建议**：（指明哪个部门）
**决定**：✅ APPROVE / 🔄 REVISE
PROMPT_EOF
# 先用 Codex；不可用则降级为 Agent 工具 model:"opus"
which codex 2>/dev/null \
  && cat /tmp/se-task/qa-reports/qa-2-result.txt | codex exec --dangerously-bypass-approvals-and-sandbox \
  || echo "⚠️ Codex 不可用，降级为 Claude Opus Agent 审查"
```

---

### 阶段三：操作员并行执行

**所有操作员一次性并行派出（独立上下文，天然隔离）：**

```
你是{角色名}，隶属于{部门名}。

项目目标：$ARGUMENTS
你的子任务：{子任务描述}
产出文件（≤3个）：{文件路径列表}
交付标准：{标准}
所在部门工作目录：/tmp/se-task/departments/{部门名}/

⚠️ 上下文保护：如果你在执行过程中感到上下文过载（产出遗漏、质量崩塌、自感混乱），
立即执行保护流程：
1. STOP — 停止当前工作
2. SAVE — 将已完成+未完成写入 /tmp/se-task/.checkpoint/{角色名}.md
3. REPORT — 在返回中明确说"上下文过载，请求接替"
4. EXIT — 结束运行

请实际产出代码/文件：
1. 用 Write 工具创建文件到工作目录
2. 完成后自己验证（运行/测试/lint）
3. 返回简短交付报告

完成后，将自己的状态写入 /tmp/se-task/.status/{角色名}.status，一行即可：
状态：✅ 完成 / ❌ 失败 / ⚠️ 过载请求接替
产出文件：N 个
备注：（如有异常简述）

完成后返回：
## 操作员交付 — {角色名}
| 产出文件 | 验证结果 |
|----------|----------|
| {文件路径} | ✅/❌ |
```

**等所有操作员完成。** 失败的操作员 → 组长接手修复。

**如果操作员报告 ⚠️ 过载请求接替：** → 项目经理执行接替流程（见「动态保护机制」章节）。

#### 各组组长验收

组长按需 Read 操作员产出文件，汇总后写入本部门 SUMMARY.md：

```
你是{部门名}组长。请验收操作员产出，汇总后写入文件。

操作员交付报告：{各操作员返回}
产出文件目录：/tmp/se-task/departments/{部门名}/

请用 Read 工具检查关键文件，然后 Write 汇总到：
/tmp/se-task/departments/{部门名}/SUMMARY.md

完成后更新自己的状态文件 /tmp/se-task/.status/{部门名}组长.status：
状态：✅ 验收完成
部门：{部门名}
```

---

### 阶段四：主管助理汇总 + 打包 + QA-3 终审

#### 4.1 派出主管助理 Agent（🆕 v4 替代主管做汇总）

**这是 v4 的关键变更：汇总工作不交给主管（避免主管上下文堆积），由专设的「主管助理」完成。**

```
你是项目主管助理。你的唯一职责是汇总信息、撰写报告。
⚠️ 你不做决策、不改代码、不参与 QA。你只做传声筒。

项目目标：$ARGUMENTS
原始部门方案：/tmp/se-task/01-department-plan.md
上下文预算表：/tmp/se-task/00-context-budget.md
各组汇总文件目录：/tmp/se-task/departments/

你需要完成：
1. 用 Read 工具逐一读取各部门的 SUMMARY.md
   （注意：用 ls /tmp/se-task/departments/ 先看有哪些部门目录，然后逐个 Read，
    不要一次性全部塞进 prompt）
2. 汇总所有成果，Write 最终报告到 /tmp/se-task/04-final-report.md
3. 将自己的状态写入 /tmp/se-task/.status/主管助理.status

报告格式：
## 🎯 项目交付报告

### 项目信息
- **目标**：...
- **团队**：N 部门 / M 人
- **主管助理**：（你的名字，本报告撰写者）

### 各部门成果
| 部门 | 产出文件 | 状态 |
|------|----------|------|

### 项目架构说明
（整体结构、关键设计决策）

### 技术栈
（各组使用的技术/库/工具）

### 如何使用
（用户如何运行/使用）

### 遗留问题和改进方向
（如实汇报）

完成后返回简短确认。
```

**主管助理在阶段五后自动解雇（不保留上下文）。**

#### 4.2 项目经理准备代码审查包

**项目经理亲自准备（主窗口操作，不开 Agent）：**

```bash
# 收集变更信息
cd {项目工作目录}
git status --short > /tmp/se-task/04-git-status.txt 2>/dev/null || echo "非 git 仓库" > /tmp/se-task/04-git-status.txt
git diff --stat > /tmp/se-task/04-diff-stat.txt 2>/dev/null || echo "无 git diff" > /tmp/se-task/04-diff-stat.txt
```

然后 Write `/tmp/se-task/04-code-package.md`，包含：
1. 任务目标
2. 主管助理交付报告路径（引用 04-final-report.md）
3. 变更统计（git status + diff stat 或等效内容）
4. 关键文件内容摘要（按模块分组）
5. 验证结果汇总
6. 主管标注的重点审查区域（如有）

#### 4.3 QA-3 终审（Codex，全新上下文）

```bash
cat > /tmp/se-task/qa-reports/qa-3-result.txt << 'PROMPT_EOF'
你是极度严格的CTO。审核项目最终交付材料。这份材料即将发给用户（非技术背景），
必须确认质量合格、内容完整、没有隐瞒问题。

任务目标：$ARGUMENTS

审查材料：
- 主管助理交付报告：/tmp/se-task/04-final-report.md
- 代码审查包：/tmp/se-task/04-code-package.md
- 变更统计：/tmp/se-task/04-diff-stat.txt
- 所有产出文件在：/tmp/se-task/departments/

请按需 Read 关键文件，不要一次性全部加载。先从报告和统计入手，
发现疑点再深入具体文件。

审核要点：
1. 交付物是否满足任务目标？
2. 代码变更是否正确？有无明显 bug 或回归风险？
3. 报告是否清晰？用户（非技术背景）能看懂吗？
4. 有没有隐藏的技术风险？
5. 遗留问题是否如实列出？

评分标准：
- 8-10分 → APPROVE
- 5-7分 → REVISE
- 1-4分 → REVISE

返回格式：
## QA 终审
**评分**：X/10
**问题**：...
**修改建议**：（指明具体部门/文件/行号）
**决定**：✅ APPROVE / 🔄 REVISE
PROMPT_EOF
# 先用 Codex；不可用则降级为 Agent 工具 model:"opus"
which codex 2>/dev/null \
  && cat /tmp/se-task/qa-reports/qa-3-result.txt | codex exec --dangerously-bypass-approvals-and-sandbox \
  || echo "⚠️ Codex 不可用，降级为 Claude Opus Agent 终审"
```

#### 4.4 处理结果

**✅ APPROVE：** 进入阶段五。

**🔄 REVISE：** 迭代回流（🆕 v4 改造）：

1. **派出主管（全新 Agent 上下文）** — 读 QA 反馈，判断哪些部门需要修改：

```
你是项目主管（REVISE 协调）。QA 终审发现以下问题，需要协调修复。

项目目标：$ARGUMENTS
QA 反馈（见文件）：/tmp/se-task/qa-reports/qa-3-result.txt
部门方案：/tmp/se-task/01-department-plan.md
各组产出目录：/tmp/se-task/departments/

操作：
1. Read QA 反馈，理解所有问题
2. 判断哪些部门需要修改、每个部门改什么
3. 返回指派清单（不要自己动手改代码）
4. 更新 /tmp/se-task/.status/主管.status

返回格式：
## REVISE 指派
| 部门 | 需修改文件 | 修改要点 |
|------|-----------|----------|
| ... | ... | ... |
```

2. 项目经理按指派清单，派出问题部门的组长 Agent（并行）：

```
你是{部门名}组长。QA 终审发现以下问题。请修复。

QA 反馈（见文件）：/tmp/se-task/qa-reports/qa-3-result.txt
你的产出目录：/tmp/se-task/departments/{部门名}/
主管指派给你的修改要点：{从 REVISE 指派清单提取}

操作：
1. Read QA 反馈，找到本部门相关的问题
2. 修改问题文件（Edit 工具）
3. 验证修改正确
4. 更新 SUMMARY.md
```

3. 修复完成 → **派出主管助理（全新 Agent）** 重新汇总（回 4.1）→ 重新打包（回 4.2）→ 再 QA（回 4.3）

**循环终止条件：**
- QA ≥ 8 分 → ✅ 通过
- 连续 2 轮评分无提升 → ⚠️ 停滞，标注风险后强制通过
- 达到 5 轮 → 🛑 强制结束，如实标注遗留问题

---

### 阶段五：项目经理交付

QA 通过后，项目经理 Read 最终报告并展示给用户：

```
## 🎯 项目交付

{/tmp/se-task/04-final-report.md 的完整内容}

**QA 终审评分**：X/10 ✅
**所有产出文件**：/tmp/se-task/departments/
```

**清理主管助理（完成传声筒使命）：** 主管助理是一次性角色，交付后自动解雇。

---

## 上下文预算管理（🆕 v4 重写）

### 三层防护体系

```
第一层：事前预算（阶段一）
  → 主管预估每个角色的上下文消耗
  → 🔴 角色必须拆解，禁止直接分配
  → 🟡 角色标注风险，准备缓解方案

第二层：事中隔离（阶段二~四）
  → 主管助理隔离汇总负载（阶段四不消耗主管上下文）
  → REVISE 时重派主管+主管助理（全新上下文，不堆积）
  → 操作员独立上下文，天然不堆积

第三层：动态保护（全阶段）
  → Agent 自感过载 → 断点存档 → 请求接替 → 不丢工作
  → 详见「动态保护机制」章节
```

### 预算模型

```
预估用量 ≈ 任务描述字数 + 需读文件大小估算 + 产出文件大小估算 + 对话轮数预留
```

**文件大小估算基准：**

| 文件类型 | 估算大小 | 示例 |
|----------|---------|------|
| 小型 | ~2K tokens | 配置文件、单函数脚本 |
| 中型 | ~5K tokens | 单组件、单模块 |
| 大型 | ~10K tokens | 完整页面、核心逻辑 |
| 汇总型 | ~8K tokens | SUMMARY.md、最终报告 |

**风险分级（以 800K 为安全阈值，1M 为硬上限）：**

| 级别 | 占用率 | 处置 |
|------|--------|------|
| 🟢 安全 | ≤60%（≤480K） | 正常分配 |
| 🟡 临界 | 61-80%（481K-640K） | 可分配，需标注缓解措施 |
| 🔴 危险 | >80%（>640K） | 禁止分配，必须拆人/加人/加部门 |

### 各角色上下文风险评级

| 角色 | 上下文来源 | v3 风险 | v4 风险 | v4 改进 |
|------|-----------|---------|---------|---------|
| 项目经理（主窗口） | 多阶段交互积累 | 🟢 极低 | 🟢 极低 | 不变 |
| 主管（Agent） | prompt + 按需 Read | 🟡 低 | 🟢 极低 | 汇总交给助理，REVISE 重派全新 |
| 主管助理（Agent） | prompt + 各组 SUMMARY | — | 🟢 极低 | 🆕 独立上下文，用完即弃 |
| 组长（Agent） | prompt + 本部门文件 | 🟢 极低 | 🟢 极低 | 不变 |
| 操作员（Agent） | prompt（≤500字） | 🟢 极低 | 🟢 极低 | 不变，新增保护机制 |
| QA 审查引擎 | 每次只收审查材料 | 🟢 极低 | 🟢 极低 | 不变 |

### 三条兜底机制

**1. 主管助理隔离汇总（🆕 v4 替代"副总管"方案）**

主管助理专责汇总，独立上下文。主管不再承担汇总负载——这消除了 v3 中主管在阶段四上下文爆炸的最大风险点。

**2. 操作员产出过多 → 强制分拆**

阶段二硬约束：每个操作员 ≤ 3 个产出文件。超出就拆任务或加人。

**3. 主窗口积累 → 重启新会话**

如果项目经理窗口因长期使用积累了过多对话历史，可以用一个新会话继续。做法：
- 把当前状态写入 `/tmp/se-task/STATE.md`（记录当前在哪个阶段、已完成什么）
- 新会话中 `/牛马公司 从 /tmp/se-task/ 恢复` 即可继续

---

## 动态保护机制（🆕 v4 新增）

### 触发条件（症状检测）

Agent 在运行过程中，项目经理**不监控实时指标**，而是依赖 Agent 自报症状：

| 症状 | 表现 | 说明 |
|------|------|------|
| 产出遗漏 | 答应写 3 个文件只写了 2 个 | 忘了承诺 |
| 质量崩塌 | 代码出现明显低级错误 | 思维混乱 |
| 自感过载 | Agent 自己觉得撑不住了 | 主动报告 |

### 保护流程：STOP → SAVE → REPORT → EXIT

Agent 检测到症状后执行：

```
STOP   — 立即停止当前工作，不再继续
SAVE   — 将当前进度写入 /tmp/se-task/.checkpoint/{角色名}.md
REPORT — 在返回中明确说"上下文过载，请求接替"
EXIT   — 结束运行
```

### Checkpoint 断点存档格式

```markdown
## {角色名} 断点存档 — {时间戳}

### 已完成
- [x] 事项1
- [x] 事项2

### 未完成
- [ ] 事项3（进度：50%，已写框架，缺测试）
- [ ] 事项4（未开始）

### 产出目录
/tmp/se-task/departments/{部门名}/

### 给接替者的话
（当前状态、注意事项、被什么卡住了）
```

### 接替流程

1. 项目经理读到 Agent 的"上下文过载，请求接替"报告
2. 项目经理 Read `/tmp/se-task/.checkpoint/{角色名}.md` 了解断点
3. 项目经理新建同名 Agent，prompt 中包含：

```
你是{角色名}（接替者）。你的前任因上下文过载中断。
请从断点继续工作，不要重做已完成的部分。

断点存档：/tmp/se-task/.checkpoint/{角色名}.md（请 Read 了解前任进度）
产出目录：/tmp/se-task/departments/{部门名}/

核心约束：只做未完成的部分，已完成的不要重做。
```

4. 接替者完成后按正常流程交付
5. 工作不能丢失是接替过程的核心约束

---

## 状态仪表盘（🆕 v4 新增）

### 机制

每个 Agent 在返回结果前，将自己的状态写入 `/tmp/se-task/.status/{角色名}.status`。

**项目经理不主动展示状态。** 只在阶段切换时口头通报一行（如"阶段三：所有操作员已交付，状态正常"）。

用户想查看时，可以自行 `cat` 查看，或问项目经理。

### 状态文件格式

一行即可，无需复杂格式：

```
状态：✅ 完成
部门：（如适用）
产出：N 个文件
备注：（异常时填写，正常可省略）
```

### 项目经理阶段通报

| 阶段 | 通报内容 |
|------|---------|
| 阶段一完成 | "部门组建通过，N部门M人，预算概况 X🟢 Y🟡" |
| 阶段二完成 | "任务拆分完成，进入执行" |
| 阶段三完成 | "所有操作员已交付" （异常时： "X正常 Y过载请求接替"） |
| 阶段四完成 | "主管助理已汇总，QA 终审评分 X/10" |
| 阶段五 | 交付报告完整内容 |

---

## 三个 QA 审计点

| 审计点 | 阶段 | 审什么 | 审查引擎 | 必须 |
|--------|------|--------|----------|------|
| QA-1 | 阶段一后 | 主管部门方案 + 上下文预算 | 可配置（Codex/Opus） | ✅ |
| QA-2 | 阶段二后 | 组长拆分方案 | 可配置（Codex/Opus） | ⚠️ 可选 |
| QA-3 | 阶段四后 | 主管助理报告 + 代码包 | 可配置（Codex/Opus） | ✅ |

**每次审查引擎调用都是全新上下文——它只看到本次的文件路径和审核指令，不携带任何历史。**

---

## 重要规则

### 角色与模型
1. **主管/主管助理/组长/操作员用 Agent 工具** → `model: "sonnet"`
2. **QA 优先用 Codex CLI** → `codex exec`；不可用时自动降级为 Agent 工具 `model:"opus"`
3. **QA 失败继续降级** → 如果 Codex 和 Opus Agent 都失败，标注风险后强制通过

### 文件中介
4. **Agent 之间传路径不传内容** → 完整内容写入 `/tmp/se-task/`，prompt 只传文件路径
5. **Agent 按需 Read** → 不要一次性把所有文件内容塞进 prompt

### QA 范围
6. **QA 审查引擎审主管级交付物** → 部门方案+预算表、拆分方案、最终报告+代码包
7. **QA 不审操作员代码细节** → 只看主管助理汇总和代码审查包中的关键片段

### 参数
8. **命令参数用 `$ARGUMENTS`**

### 产出文件
9. **操作员每人 ≤ 3 个产出文件** → 超出则组长在阶段二拆分

### 上下文保护（🆕 v4）
10. **阶段一主管必须输出上下文预算表** → `/tmp/se-task/00-context-budget.md`
11. **🔴 危险角色禁止直接分配** → 必须在阶段一拆人或加人
12. **汇总由主管助理完成** → 主管不参与阶段四汇总
13. **REVISE 时重派主管+主管助理** → 均为全新上下文，不堆积历史
14. **Agent 自感过载立即 STOP + SAVE + REPORT + EXIT** → 不要硬撑
15. **接替者从断点继续** → 不重做已完成部分，不丢工作

### 状态文件（🆕 v4）
16. **每个 Agent 返回前写 .status 文件** → 一行即可
17. **项目经理只在阶段切换时通报** → 不主动展示，用户问才看
18. **主管助理交付后自动解雇** → 下一次任务重新招聘

### 任务类型 → 部门策略参考

| 任务类型 | 建议部门 | 典型人数 | QA-2 |
|----------|----------|----------|------|
| Bug 修复 | 前端+后端+测试 | 3-5人 | 跳过 |
| 新功能 | 前端+后端+测试+文档 | 5-10人 | 必须 |
| 代码重构 | 后端+测试 | 3-5人 | 跳过 |
| 技术调研 | 数据组+文档组 | 2-4人 | 跳过 |
| 全栈项目 | 前端+后端+测试+文档 | 8-15人 | 必须 |
| 后续迭代 | 参照项目记忆文件 | 按需 | 跳过 |

---

## 后续更新模式

### 核心思路：每次更新都是独立任务

**不需要保留上一轮的对话历史。** 代码本身已经是团队最完整的"项目文档"。

```
第一轮：/牛马公司 做一个博客系统 → 产出 /Users/mac/my-blog/
       → 自动生成 /Users/mac/my-blog/PROJECT.md（项目记忆文件）

第二轮：/牛马公司 在 /Users/mac/my-blog 里加评论功能，支持匿名评论和回复
       → 项目经理开全新会话
       → 主管先派出"调研组"Read PROJECT.md + 关键代码结构
       → 然后按正常流程组建部门、开发、Codex审查、交付
       → 自动更新 PROJECT.md
```

### 项目记忆文件（PROJECT.md）

每次任务完成后，主管助理自动生成/更新项目根目录下的 `PROJECT.md`。它不是给计算机读的配置，而是**给下一轮主管快速了解项目的手册**：

```
## PROJECT.md（由牛马公司 v4 自动生成）

### 项目概述
{一句话描述这个项目是做什么的}

### 技术栈
- 前端：...
- 后端：...
- 数据库：...
- 部署：...

### 目录结构
```
/Users/mac/my-blog/
├── frontend/          # React + Tailwind 前端
├── backend/           # Express + SQLite 后端
├── tests/             # 测试用例
└── PROJECT.md         # 本文件
```

### 关键架构决策
- 为什么用 SQLite 而非 PostgreSQL：...
- API 路径统一以 /api/v1 开头
- 认证方式：...

### 历史记录
| 日期 | 任务 | QA评分 | 产出摘要 |
|------|------|--------|----------|
| 2026-06-10 | 初始构建，博客系统 v1 | 8.5/10 | 前端+后端+测试 共 15 文件 |
| 2026-06-12 | 新增评论功能 | 9/10 | 新增 5 文件，修改 3 文件 |
```

### 后续更新的主管 prompt 差异

后续更新时，阶段一的主管 prompt 增加了"先探查现有项目"的步骤：

```
你是项目主管。这是一个**后续迭代任务**，已有项目代码在磁盘上。

任务目标：$ARGUMENTS（包含项目路径和修改需求）

⚠️ 执行前先做三件事：
1. Read 项目根目录的 PROJECT.md（如果存在）
2. Bash 列出项目目录结构（ls -R 或 tree）
3. 理解现有架构后，再决定需要哪些部门

然后按正常流程：组建部门（含上下文预算分析）→ 等待 QA 审计 → 执行。
```

### 新项目 vs 后续迭代的自动判断

项目经理在阶段零自动判断：
- 任务目标包含文件路径特征（如 `/Users/`、`/home/`、盘符 `C:\` 以及具体项目目录）→ **后续迭代模式**
- 任务目标是纯功能描述（如"做一个博客系统"）→ **首次构建模式**

---

## 恢复中断

如果项目经理窗口（主对话）因长期使用或意外中断导致上下文过长，可以启动新会话恢复。

### 紧急保存

当前会话中告诉项目经理：

```
保存进度
```

项目经理将当前状态写入 `/tmp/se-task/STATE.md`：

```
## 牛马公司进度
**任务目标**：...
**当前阶段**：阶段四（等待 QA-3 结果）
**已完成**：部门矩阵、组长拆分、操作员代码产出
**工作目录**：/tmp/se-task/
**项目目录**：/Users/mac/my-blog/
**最后操作**：已提交 QA-3，等待 Codex 返回结果
```

### 从新会话恢复

```
/牛马公司 从 /tmp/se-task/ 恢复
```

项目经理读到 STATE.md，知道做到哪了，直接从断点继续，不需要重跑。

### 恢复判断

项目经理在阶段零判断：
- 包含 `恢复` 关键词 + 路径 → **恢复模式**，读取 STATE.md
- 包含项目路径 + 功能描述 → **后续迭代模式**
- 只有功能描述 → **首次构建模式**
